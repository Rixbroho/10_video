<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="stylesheets/style.css">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="w-full h-screen bg-zinc-900 p-10 text-white">
        <div class="nav m-10 text-blue-500">
            <a href="/read">Read</a>
        </div>
        <h1>Update User</h1>
        <form action='/update/<%= user._id %>' method="post">
            <input class="px-5 py-3 border-2 border-zinc-800 rounded-lg outline-none bg-transparent" type="text" name="user_name" placeholder="name" value="<%= user.username %>" required>
            <input class="px-5 py-3 border-2 border-zinc-800 rounded-lg outline-none bg-transparent" type="text" name="user_email" placeholder="email" value="<%= user.email %>" required>
            <input class="px-5 py-3 border-2 border-zinc-800 rounded-lg outline-none bg-transparent" type="text" name="image_url" placeholder="image url" value="<%= user.image %>" required>
            <input class="px-5 py-2 bg-yellow-500 rounded-lg ml-3" type="submit" value="Update User">
        </form>
    </div>
</body>
</html>